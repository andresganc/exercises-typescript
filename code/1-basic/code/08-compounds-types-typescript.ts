
// TYPES COMPUESTOS SOLO TYPESCRIPT: Se accede a la referencia del valor

// Tuples ( Solo en Typescript)
    // Tupla string y number
    let db1: [string, number] = ['Marco', 3]

    // Tupla string y number con apodo (Desde la version 4 de typescript)
    let db2: [nombre: string, edad: number, mayor: boolean] = ['Marco', 3, false]

    // Tupla con varios registros
    /*
    let db3: [nombre: string, edad: number] = [
        ["Marco2", 3],
        ["Mariana", 16],
        ["Daniela", 17],
        ['Alejandra', 33],
        ['Andres', 42]
    ]
    */